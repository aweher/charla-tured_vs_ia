---
# Logs del host (montado vía /var/log:/var/log:ro en docker-compose)
filenames:
  - /var/log/auth.log
  - /var/log/syslog
labels:
  type: syslog
---
# Si tenés Nginx en el host, descomenta:
# filenames:
#   - /var/log/nginx/access.log
#   - /var/log/nginx/error.log
# labels:
#   type: nginx
